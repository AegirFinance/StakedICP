import React from "react";
import { styled } from "../stitches.config";

const Wrapper = styled('div', {});

// TODO: Implement this
export function ActivityIndicator(props: Parameters<typeof Wrapper>[0]) {
  return <Wrapper {...props}>...</Wrapper>;
}
