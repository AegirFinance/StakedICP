import React from "react";
import { styled } from "../stitches.config";

export const Input = styled('input', {
  color: '$slate12',
  borderColor: '$slate7',
  borderRadius: '$1',
  borderWidth: '$1',
  outline: 'none',
  boxShadow: 'none',
  background: '$slate1',
  padding: '$2 $3',
});
